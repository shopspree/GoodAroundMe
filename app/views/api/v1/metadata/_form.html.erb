<%= form_for(@api_v1_metadatum) do |f| %>
  <% if @api_v1_metadatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_v1_metadatum.errors.count, "error") %> prohibited this api_v1_metadatum from being saved:</h2>

      <ul>
      <% @api_v1_metadatum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
