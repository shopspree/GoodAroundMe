# config/settings.yml

# general
application.name: 'Good Around Me'

# authentication
authentication.approval_required: false

# context
context.global.name: 'GLOBAL'
context.global: true

# follow
follow.default.organization: true
follow.default.team: false